-- scan.lua
-- a simple script that presents a view of the whole band 

-- run once upon startup to set initial state
local function init ()
end

-- run periodically
local function run()
end



return {run=run, init=init}